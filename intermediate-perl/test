#!/usr/local/bin/env perl
# use strict;
# use warnings;
use feature qw(say state current_sub);
use IO::Dir;
# opendir my $dh, '.';
my $dh = IO::Dir->new('.');
foreach my $file (readdir($dh)) {
    say $file;
}
$dh->rewind;
foreach my $file (readdir($dh)) {
    say $file;
}
